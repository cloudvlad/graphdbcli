## Objective
Create a command with subcommands that will allow us to manage the repositories in GraphDB using the GraphDB API itself.

## Decision
- [[Common command with persistent flags]]
- Implement each operation as an individual subcommand.
