package versioncmd

var examples = `Check the examples from the sub-commands below`

var shortDescription = `Manages GraphDB versions`

var longDescription = `Manages GraphDB version by using Inter-Planetary Filesystem (IPFS)
for fetching the Platform Independent Distribution (PID) files. 

Each distribution file can be verified by its precalculated SHA-256 checksum.
`
