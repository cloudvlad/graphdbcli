package workbenchcmd

var examples = `Check the examples from the sub-commands below`

var shortDescription = `Manages GraphDB Workbench instances`

var longDescription = `Manages GraphDB Workbench instances

All workbenches are stored in the home directory of the CLI tool (~/.gdb) 
in subdirectory workbenches/.
`
