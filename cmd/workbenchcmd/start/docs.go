package start

var examples = `Check the examples from the sub-commands below`

var shortDescription = `Starts the custom GraphDB Workbench`

var longDescription = `Starts the custom GraphDB Workbench

Executes dependency installation and runs the workbench once finished.
The exact performed actions are 'npm run clean-install' and 'npm run start'.

Once started the process logs will be stored in two separate files, for normal output and errors - 
instance.out.log and instance.err.log that are stored inside the instance directory itself.
`
