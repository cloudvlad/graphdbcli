package cmd

var shortDescription = `GraphDB Command Line Tool`
var longDescription = `GraphDB Command Line Tool

The CLI is used to manage different resources, like licenses, 
configuration files and others, as well as processes like 
GraphDB instance creation and backup creation.`

var examples = `graphdbcli -h`
