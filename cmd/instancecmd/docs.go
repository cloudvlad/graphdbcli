package instancecmd

var examples = `Check the examples from the sub-commands below`

var shortDescription = `Manages GraphDB instances`

var longDescription = `Manages GraphDB instances, enabling operations like
starting, stopping and destroying/purging GraphDB instances.`
